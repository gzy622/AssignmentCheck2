---
trigger: always_on
---

## After completing any code modification task, you must **automatically**:
- Generate a Git commit message that complies with the **Conventional Commits** standard, and output it in a code block as plaintext
- Write the Git commit message in Simplified Chinese by default.