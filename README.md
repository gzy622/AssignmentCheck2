# 作业极速登记 Pro

一个轻量化、模块化的课堂作业登记工具，面向教师在课上快速标记作业提交状态和分数。本项目无需构建，直接在浏览器中运行，兼顾高效登记与数据安全。

> 当前版本: `1.0.2 (20260317)`

## 功能概览

- **网格化点名登记**：按学号/姓名展示学生卡片，点击即可切换“已完成”状态。
- **分数登记模式**：开启“打分”后可逐个录入学生分数，支持快速设为 `0`、`100`、`+1`、`-1`，并联动提交状态。
- **多任务管理**：支持新增、切换、重命名、删除任务，并可维护任务科目标签。
- **名单编辑**：支持批量编辑学生名单（每行一条：`学号 空格 姓名`），可为个别学生添加排除标记。
- **统计视图**：支持按多个任务汇总查看班级平均完成率、学生完成率和逐项提交明细。
- **调试面板 (Debug Panel)**：内置开发者调试工具，支持实时日志查看、日志级别筛选、生命周期监控，并支持在界面上自由拖拽。
- **本地持久化**：数据保存在浏览器 `localStorage`，刷新页面后仍保留。
- **备份导入导出**：支持导出为 `assignmentcheck2_backup_YYYYMMDD.json`，导入时会校验数据结构后再覆盖现有数据。
- **动画开关**：可切换界面动画效果，适配低性能设备。
- **移动端适配**：网格会根据视口自动调整尺寸，弹窗针对移动端输入做了专门处理。

## 快速开始

本项目由多个原生 JavaScript 模块组成，无需安装 Node.js 或构建工具，直接在浏览器中打开 `index.html` 即可使用。

## 测试说明

本项目包含完善的单元测试和端到端（E2E）测试。

### 安装测试环境

```bash
npm install
npx playwright install chromium
```

### 运行测试

- **运行所有测试**: `npm run test:all`
- **运行单元测试 (Vitest)**: `npm run test`
- **运行端到端测试 (Playwright)**: `npm run test:e2e`
- **生成覆盖率报告**: `npm run test:coverage`

### 测试结构

- `core.test.js`: 基础工具函数与数据读写测试。
- `tests/state.test.js`: 应用状态管理逻辑测试。
- `app.spec.js`: Playwright 端到端用户核心流程测试。
- `tests/e2e_present_mode.spec.js`: 展示模式 E2E 测试。
- `tests/e2e_preset.spec.js`: 预设任务与配置 E2E 测试。

## 项目结构

```text
.
├── index.html       # 应用入口文件
├── styles.css       # 全局样式文件
├── core.js          # 核心工具类与调试逻辑 (DOM, LS, Debug)
├── app.js           # 应用全局状态管理与核心逻辑
├── modal.js         # 通用弹窗组件管理
├── action-views.js  # UI 视图生成逻辑
├── actions.js       # 业务操作分发逻辑
├── back-handler.js  # 历史记录与返回键拦截处理
├── boot.js          # 应用引导程序
├── AGENTS.md        # 仓库内协作与输出约束
└── README.md
```

## 适用场景

- 课堂作业即时登记
- 随堂小测提交追踪
- 需要离线、快速、轻量化记录的教学场景
